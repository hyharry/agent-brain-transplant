[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-brain-transplant"
version = "0.1.0"
description = "Backup and transplant OpenClaw/Hermes agent brains"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
